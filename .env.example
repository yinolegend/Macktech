# Production Environment Configuration Example
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
NODE_ENV=production

# JWT Secret - MUST be changed in production!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# JWT Token Expiration
JWT_EXPIRES_IN=12h

# Database Configuration
# SQLite database will be created in ../data/app.db by default
# DB_PATH=../data/app.db

# CORS Configuration (optional)
# Set allowed origins for CORS, comma-separated
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Active Directory / LDAP Configuration (optional)
# AD_URL=ldap://your-ad-server.com
# AD_BIND_DN=CN=Service Account,OU=Users,DC=company,DC=com
# AD_BIND_PASSWORD=service_account_password
# AD_SEARCH_BASE=OU=Users,DC=company,DC=com
# AD_SEARCH_FILTER=(sAMAccountName={{username}})

# Logging
# LOG_LEVEL=info
